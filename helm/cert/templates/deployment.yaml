{{- if .Values.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "cert.fullname" . }}
  namespace: {{ .Values.namespaceOverride | default .Release.Namespace }}
  labels:
    {{- include "cert.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "cert.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "cert.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "cert.serviceAccountName" . }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        fsGroup: 10001
      containers:
        - name: {{ include "cert.fullname" . }}
          image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8093
          envFrom:
            - configMapRef:
                name: {{ include "cert.fullname" . }}-config
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "cert.fullname" . }}-secrets
                  key: DATABASE_URL
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          livenessProbe:
            exec:
              command: ["/app/scripts/healthcheck.sh"]
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8093
            periodSeconds: 10
          startupProbe:
            httpGet:
              path: /healthz
              port: 8093
            failureThreshold: 30
            periodSeconds: 2
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
{{- end -}}
