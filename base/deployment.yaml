apiVersion: apps/v1
kind: Deployment
metadata:
  name: cert
  namespace: cert
  labels:
    app.kubernetes.io/name: cert
    app.kubernetes.io/part-of: arca-suite
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: cert
  template:
    metadata:
      labels:
        app.kubernetes.io/name: cert
        app.kubernetes.io/part-of: arca-suite
    spec:
      serviceAccountName: cert
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        fsGroup: 10001
      containers:
        - name: cert
          image: server01:5000/arca-suite/arca-cert:0.1.8
          ports:
            - containerPort: 8093
          envFrom:
            - configMapRef:
                name: cert-config
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: cert-secrets
                  key: DATABASE_URL
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          # The image ships neither curl nor the scripts/ directory: probe
          # the app's own HTTP endpoint instead.
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8093
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8093
            periodSeconds: 10
          startupProbe:
            httpGet:
              path: /healthz
              port: 8093
            failureThreshold: 30
            periodSeconds: 2
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi
